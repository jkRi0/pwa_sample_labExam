const OfflinePage = () => (
  <div className="container">
    <section className="card">
      <h1 className="card__title">You are offline</h1>
      <p>
        Recent coaching drills are still available thanks to offline caching. Once your connection returns we will
        sync the latest information.
      </p>
    </section>
  </div>
);

export default OfflinePage;
